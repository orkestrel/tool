# Tool

> **Under construction.** This package ships concrete, LLM-callable tools for the
> `@orkestrel` line — workflow authoring, workspace editing, and sub-agent delegation —
> built over [`@orkestrel/agent`](agent.md)'s `ToolInterface` / `createTool` runtime, with
> pluggable stores. Source: [`src/core`](../../src/core). Surfaced through the `@src/core`
> barrel.

## Surface

_To be documented as each concrete tool lands._

## Methods

_To be documented as each concrete tool lands._

## Patterns

_To be documented as each concrete tool lands._

## Tests

_To be documented as each concrete tool lands._

## See also

- [`agent.md`](agent.md) — the `ToolInterface` / `ToolManager` runtime these tools plug into.
- [`workflow.md`](workflow.md) — the workflow primitives the workflow-authoring tool wraps.
- [`contract.md`](contract.md) — the shape DSL a tool's `operation`-discriminated contract compiles through.
